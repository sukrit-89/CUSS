[package]
name = "rerail_registry"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
soroban-sdk = "26.0.0-rc.1"

[dev-dependencies]
soroban-sdk = { version = "26.0.0-rc.1", features = ["testutils"] }
